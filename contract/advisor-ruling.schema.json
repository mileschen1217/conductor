{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "advisor-ruling",
  "x_notes": {
    "created": "2026-07-11",
    "validation_target": "the payload of an advisor_ruling journal line, after stripping the journal envelope keys 'event' and 'moment_id' (envelope vocabulary is audited by the judgment-flow audit, not this schema)",
    "digest_ref": "optional at this layer to stay vendor-neutral; a binding may require it (enforcement home: that binding's text + the portability AC's retained-digest check)"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["decision_type", "ruling", "rationale", "confidence", "what_would_change_my_mind"],
  "properties": {
    "decision_type": { "type": "string", "enum": ["entry-gate", "grading-dispute", "worker-blocked", "acceptance-ambiguity", "scope-change-preview"] },
    "ruling": { "type": "string" },
    "rationale": { "type": "string" },
    "confidence": { "type": "string", "enum": ["high", "medium", "low"] },
    "what_would_change_my_mind": { "type": "string" },
    "digest_ref": { "type": ["string", "null"] }
  }
}
