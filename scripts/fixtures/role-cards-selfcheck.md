# role-cards self-test fixture — planted judgment-face terms

This file exists ONLY for `check-role-cards.sh --self-test` (proving the
scanner detects judgment-face literals). It is never scanned by the real
gate (which reads contract/roles/ only).

Planted terms: this card issues the final verdict, may approve scope, holds
permission to adjudicate disputes, records the entry ruling, can promote a
acceptance verdict, applies a contract change, and may refuse a task.
