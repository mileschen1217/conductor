# Neutrality self-check fixture

Planted banned nouns — this file exists so `check-neutrality.sh --self-test`
can prove the scanner is alive. It lives under `scripts/fixtures/`, OUTSIDE
the scan scope (doctrine/ + contract/). Never move it into scan scope.

claude anthropic codex openai gpt opus sonnet haiku subagent AGENTS.md SKILL.md MCP TOML
